import React from "react";
import "../Header/Header.css"; // Optional for styling

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">Magizhl Mart</h1>
    </header>
  );
};

export default Header;
